# --- Core Console & System ---
CONFIG_PRINTK=y
CONFIG_STDOUT_CONSOLE=y
CONFIG_REBOOT=y

# --- UI Module (UART) ---
CONFIG_SERIAL=y
CONFIG_UART_ASYNC_API=y

# --- Hardware ---
CONFIG_I2C=y

# --- Floating Point for printk (Lux = %.1f) ---
CONFIG_CBPRINTF_FP_SUPPORT=y